"""SourceWeave generation pipeline.

Reads pinned upstream source, drafts documentation grounded in it, and writes
reviewed-ready Markdown with provenance into ``targets/<name>/docs/``.
"""

__version__ = "0.1.0"
